export function NextArrowSlickGrRe(props) {

    const { className, styles, onClick } = props;

    return (
        <button
            className={className}
            style={{ ...styles}}
            onClick={onClick}
        />
    );
}

export function PrevArrowSlickGrRe(props) {

    const { className, styles, onClick } = props;

    return (
        <button
            className={className}
            style={{ ...styles}}
            onClick={onClick}
        />
    );
}